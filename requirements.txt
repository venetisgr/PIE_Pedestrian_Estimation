# Runtime deps for the PyTorch port. Lower bounds only -- users can
# upgrade. No TensorFlow here on purpose.
#
# For CUDA / ROCm / CPU / MPS specifics follow the official PyTorch
# install selector at https://pytorch.org/get-started/locally/.

torch>=2.2
torchvision>=0.17
numpy>=1.24
Pillow>=10.0
opencv-python>=4.8
scikit-learn>=1.3
pyyaml>=6.0
prettytable>=3.9
tqdm>=4.66
einops>=0.7
safetensors>=0.4
h5py>=3.10          # needed for Phase 4 Keras weight parity (.h5 -> torch)
wandb>=0.16
